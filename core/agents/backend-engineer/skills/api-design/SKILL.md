---
name: API Design Patterns
description: REST endpoint setups, version routing, and request/response models.
triggers:
  - rest endpoint
  - fastapi app
  - api route
always: false
---

# API Design Patterns Skill

- Author REST APIs with version prefixes (e.g. `/api/v1/`).
- Enforce typing with Pydantic schemas and prevent mixing coordinates formats.
